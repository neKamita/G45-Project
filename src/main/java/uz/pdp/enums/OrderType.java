package uz.pdp.enums;

/**
 * Types of orders that can be placed.
 * Every order has its own style! 🎨
 */
public enum OrderType {
    FULL_SET,    // Complete door set
    POLOTNO      // Door canvas only
}
